;; -*- mode: emacs-lisp ; coding: utf-8-unix -*-
;; ~/.emacs.d/el-get-init-files/init-ac-python.el
;; Last modified: 2013/03/31 18:48:22

;; ------------------------------------------------------------------------
;; ac-python

;; (when (require 'ac-python nil 'noerror)
;;   ;; ac-python は python-symbol-completions を呼び出す
;;   ;; 現状の python-mode では py-symbol-completions という名前になっているため
;;   ;; ラッパーを定義
;;   (defun python-symbol-completions (symbol)
;;     (py-symbol-completions symbol))
;;   )

